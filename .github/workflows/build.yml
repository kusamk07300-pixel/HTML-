name: Android CI Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Install System Gradle
      run: |
        sudo apt-get update
        sudo apt-get install -y gradle

    - name: Fix Project Configuration & Remove Kotlin Dependency Clash
      run: |
        if [ ! -f settings.gradle ]; then
          echo "include ':app'" > settings.gradle
        fi
        # build.gradle से kotlin plugin लाइन हटाना ताकि NoClassDefFoundError न आए
        find . -name "build.gradle" -exec sed -i '/kotlin/d' {} + || true

    - name: Build Debug APK with System Gradle
      run: gradle assembleDebug --stacktrace || gradle generateDebugSources --stacktrace

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: app-debug-apk
        path: app/build/outputs/apk/debug/app-debug.apk
        
